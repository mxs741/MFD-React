export enum FontSize {
  xs = 12,
  sm = 14,
  md = 16,
  lg = 20,
  xl = 24,
}

export enum Radius {
  xs = 0,
  sm = 0.25,
  md = 0.5,
  lg = 0.8,
  xl = 2,
}

export enum Padding {
  xs = "1.4rem",
  sm = "1.6rem",
  md = "1.8rem",
  lg = "2rem",
  xl = "2.6rem",
}
